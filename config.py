"""
Configuration — LEGACY FILE.

All zone data, schedules, safety settings, and weather thresholds have been
moved to the SQLite database (sprinkler.db), managed by database.py.

On first run, database.py seeds the DB with the same zone definitions and
settings that were previously defined here.

This file is kept for reference only. Nothing imports from it.
"""
